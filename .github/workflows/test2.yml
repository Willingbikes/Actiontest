name: Scheduled Ping
on:
  schedule:
    - cron:  '*/30 * * * *'
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  pinger_job:
    name: A job to ping
    runs-on: ubuntu-latest
    env:
      URL: ${{ 210.61.117.99 }}
      METHOD: GET 
    steps:
      - id: ping
        uses: sisodiya2421/pinger@master
